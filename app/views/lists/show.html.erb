<div class="page-header">
  <h1><%= @list.name %></h1>
  <h3><%= link_to "Rename this List", edit_list_path(@list) %> | <%= link_to "Delete this list", @list, method: :delete, data: { confirm: "Are you sure?" } %></h3>
</div>